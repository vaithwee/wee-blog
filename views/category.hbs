{{> header}}
<div class="container">
    <div class="row">
        <div class="col-lg-6 col-lg-offset-3 col-md-4 col-md-offset-4 text-center" style="margin-bottom: 60px">
            {{#each data}}
                <a href="category/{{_id}}"><h3>#{{name}}#</h3></a>
            {{/each}}
        </div>
    </div>
</div>
